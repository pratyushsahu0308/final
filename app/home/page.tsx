import Home from "@/components/Home";
import React from "react";

export const metadata = {
  title: "Home",
};

const HomePage = () => {
  return (
    <div>
      <Home />
    </div>
  );
};

export default Home;
